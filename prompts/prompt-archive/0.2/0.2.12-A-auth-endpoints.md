# Prompt Archive: 0.2.12-A — Implement Auth Endpoints (Real Routes)

## Original Task
Implement real auth endpoints (login/signup/logout/reset-request/reset-confirm/settings) via server routes/API; align with `docs/api/auth-api.md`.

## Restatement
Build backend/server routes for auth: login, signup, logout, reset request, reset confirm, and settings update. Follow the auth API spec, emit non-revealing errors, and prepare for HTTP-only cookie sessions. Provide stub data storage if a real DB isn’t ready.

## Phase/Sub-phase
0.2.12-A

## Clarifications
None yet.

## Plan
- Add server routes/handlers for login/signup/logout/reset-request/reset-confirm/settings.
- Validate inputs per `docs/api/auth-api.md`; return structured errors.
- Issue session/token placeholder and set HTTP-only cookie (or stub) for now; avoid localStorage.
- Stub persistence (in-memory) until DB/migrations land; keep code modular to swap in real data layer.

## Outcomes
- Implemented Next.js route handlers for login/signup/logout/reset-request/reset-confirm/settings using Prisma + SQLite, hashed passwords, structured errors, and HTTP-only session cookies (30d TTL).  
- Added in-memory-to-SQLite migration path via Prisma schema; reset tokens persisted with expiry; logout clears cookie and session.  
- Frontend ready to point at real routes (no localStorage; cookie-based auth).***
