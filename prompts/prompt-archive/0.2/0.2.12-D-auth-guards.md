# Prompt Archive: 0.2.12-D — Logout Flow & Auth Guards

## Original Task
Add logout flow (UI + API) and route guards (stub) for protected pages.

## Restatement
Implement a logout action in the UI that calls the real logout endpoint, clears the session/CSRF cookies, and redirects to login. Add a simple auth guard helper for protected pages (stubbed to check session) to prep for gating protected routes.

## Phase/Sub-phase
0.2.12-D

## Clarifications
None yet.

## Plan
- Expose a logout action/button that calls `/api/auth/logout`, then redirects to login.
- Add a guard helper (server/client as needed) that checks session and redirects to login if unauthenticated (stub: check session cookie/endpoint).
- Document usage and note that full protection will harden in later phases.

## Outcomes
- Logout flow wired to API with redirect to login; stub auth guard helper added for protected routes; prompt archive updated.***
