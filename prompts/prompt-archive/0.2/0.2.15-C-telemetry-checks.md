# Prompt Archive: 0.2.15-C — Telemetry Schema Checks & Docs

## Original Task
Add basic CI check for telemetry schema (stubbed JSON schema) and update docs/changelog.

## Restatement
Introduce an automated check that validates telemetry payloads against the schema (as a CI step), and document the telemetry contract and workflow, updating the changelog accordingly.

## Phase/Sub-phase
0.2.15-C

## Clarifications
None yet.

## Plan
- Add a script/CI check that validates telemetry payloads against the schema (stub JSON schema if needed).
- Document the telemetry contract and how to extend it; update changelog and outline references.
- Keep the check non-blocking for local dev if desired, but enforce in CI.

## Outcomes
- Telemetry schema validation is automated in CI, and the telemetry contract is documented with changelog entry.
