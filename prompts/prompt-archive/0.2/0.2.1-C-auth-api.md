# Prompt Archive: 0.2.1-C — Auth API Spec

## Original Task
Proceed with 0.2.1-C: Define auth API spec (routes, payloads, validation, error states).

## Restatement
Document the auth API for 0.2: routes/payloads/validation/error states for login, signup, reset flows, logout, and session introspection. Ensure alignment with the data model and session lifecycle. Scope is documentation only; no code changes.

## Phase/Sub-phase
0.2.1-C

## Clarifications
None yet.

## Plan
- Review current `docs/api/auth-api.md` and expand/clarify routes, payloads, validation, and error catalog for 0.2.
- Ensure spec aligns with session/token lifecycle and single-tenant context.
- Keep changes limited to documentation.

## Outcomes
- Expanded `docs/api/auth-api.md` for 0.2:
  - Signup includes consent/analytics fields; responses carry consentVersion/analyticsOptIn.
  - Session introspection includes consent fields; logout auth clarified.
  - Validation table updated; token/session guidance prefers secure cookies with CSRF protections and hashed session tokens if persisted.
- No code changes; tests N/A (docs update).***
