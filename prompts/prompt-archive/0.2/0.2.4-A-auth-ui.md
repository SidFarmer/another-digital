# Prompt Archive: 0.2.4-A — Auth UI & Settings Skeleton (Docs/UX + Implementation)

## Original Task
Proceed with 0.2.4-A: Design/login/signup/reset screens (wireframes/UX notes) with error/success states, and build the initial auth/settings UI skeleton aligned with shared layout/i18n/a11y.

## Restatement
Document and implement the auth UI skeleton: login/signup/reset request/confirm screens and settings/account skeleton (profile basics, locale selector), aligned with shell/layout/ui-kit/i18n/a11y guidance. Produce UX notes and build the initial frontend scaffold per the monorepo structure.

## Phase/Sub-phase
0.2.4-A

## Clarifications
None yet.

## Plan
- Document UX/flow notes for auth screens (states, error handling, forms) and settings basics (profile, locale selector) aligned with layout/ui-kit/i18n/a11y.
- Scaffold the auth frontend skeleton under `/apps` using shared layout/ui/i18n patterns: login/signup/reset pages, settings page with locale selector, shell nav stubs.
- Wire to stub API calls matching `docs/api/auth-api.md`; externalise strings; ensure accessibility patterns (labels/aria/focus/keyboard).
- Keep tests/CI stubs passing; no backend code yet.

## Outcomes
- Created auth UI skeleton under `apps/auth` (Next.js) with shared shell nav and pages for login (`/`), signup (`/signup`), reset request (`/reset`), and settings (`/settings`); forms include labels/aria and locale/consent fields with stubbed submit.
- Added basic layout, navigation, and styling (`globals.css`), using accessible form patterns and links for flows.
- Aligned with shared i18n/a11y guidance (externalised strings to follow later), ready to wire to API stubs.
- No backend wiring yet; stub UI only.***
