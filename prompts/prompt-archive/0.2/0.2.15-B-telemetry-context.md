# Prompt Archive: 0.2.15-B — Telemetry Context & Resilience

## Original Task
Include locale/tenant/consent/analytics flags in events; add error handling/backoff stubs.

## Restatement
Enrich telemetry events with structured context (locale, tenant id, consent/analytics flags) and harden the client with retry/backoff hooks and graceful error handling.

## Phase/Sub-phase
0.2.15-B

## Clarifications
None yet.

## Plan
- Add locale, tenant id, consent, and analytics flags to telemetry payloads in a consistent schema.
- Wire retry/backoff hooks (configurable) and robust error handling so auth flows stay unaffected by telemetry failures.
- Validate the added context fields and document the behavior.

## Outcomes
- Telemetry events carry the required context, are validated, and the client handles failures with retry/backoff hooks without impacting auth flows.
