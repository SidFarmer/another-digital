# Prompt Archive: 0.2.2-C — Migration/Versioning Discipline (Auth)

## Original Task
Proceed with 0.2.2-C: define migration/versioning discipline for auth-related tables once code begins.

## Restatement
Document the approach for managing migrations/versioning for auth tables (users, tenants, sessions) as code is introduced: naming/versioning conventions, doc-first updates, CI checks, and rollback/forward expectations. Scope is documentation only; no code changes.

## Phase/Sub-phase
0.2.2-C

## Clarifications
None yet.

## Plan
- Add migration/versioning discipline notes to `docs/data-model/migrations.md` (or a dedicated note) covering naming, doc-first process, CI checks, and rollback expectations for auth tables.
- Keep scope to documentation; no migration tooling yet.

## Outcomes
- Added migration/versioning discipline to `docs/data-model/migrations.md`: naming/versioning convention (timestamp + scope), doc-first linkage to schema/table docs, CI expectations (lint/validate/apply/revert once tooling exists), and rollback guidance for auth migrations (session/token rotation safety). Tests: N/A (docs update).***
