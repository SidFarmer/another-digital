# Prompt Archive: 0.2.5-B — Formatting Utilities & Language Switcher Behavior

## Original Task
Proceed with 0.2.5-B: Document formatting utilities (dates/numbers) and language switcher behavior in shell and settings.

## Restatement
Define how date/number formatting will be handled via shared i18n utilities and specify expected behavior for the language switcher in shell/settings (default, persistence, scope). Scope is documentation only; no code changes.

## Phase/Sub-phase
0.2.5-B

## Clarifications
None yet.

## Plan
- Add notes to frontend docs (or i18n plan) describing formatting helpers (dates/numbers) and where they live (shared i18n utils).
- Document language switcher behavior in shell/settings: default locale, persistence, scope (app-wide), and fallback.
- Keep changes to documentation.

## Outcomes
- Added formatting/switcher notes to `docs/frontend/README.md`: shared i18n package provides date/number formatting; avoid in-component formatting; language switcher in shell/settings updates persisted preference, reloads bundles, defaults to `en-US` if unset/unsupported, and falls back on missing keys.
- No code changes; tests N/A (docs update).***
