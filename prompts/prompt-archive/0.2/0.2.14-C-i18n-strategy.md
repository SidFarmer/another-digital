# Prompt Archive: 0.2.14-C — i18n Loading Strategy & Hardcoded-String Guard

## Original Task
Document i18n loading strategy (per-app bundles, fallback) and add lint/check to prevent hardcoded strings.

## Restatement
Describe how i18n bundles are loaded per app with fallbacks, and put a guard in place (lint/check) to block hardcoded strings or missing keys.

## Phase/Sub-phase
0.2.14-C

## Clarifications
None yet.

## Plan
- Document current i18n structure (shared messages, per-app usage, default locale/fallback).
- Ensure lint/check (e.g., `i18n:check`) runs to detect missing keys/hardcoded strings; note how to run it.
- Outline how to add new locales/bundles and keep keys in sync.

## Outcomes
- i18n loading/fallback documented in i18n README; hardcoded-string/missing-key guard noted via `pnpm i18n:check`; prompt archived.***
