# Prompt Archive: 0.2.13-B — Auth Loading/Disable & Error/Redirect Polish

## Original Task
Add loading states/spinners and disable submit during requests; ensure non-revealing auth errors; ensure redirect targets are correct and configurable.

## Restatement
Polish auth flows (login/signup/reset/settings) by tightening loading/disabled states, keeping auth errors non-revealing, and aligning redirects/messages to the intended targets. Add any missing i18n strings needed for these states.

## Phase/Sub-phase
0.2.13-B

## Clarifications
None yet.

## Plan
- Ensure all auth submit buttons show loading text/spinner and disable during requests.
- Confirm auth error messages stay non-revealing; externalise any missing strings.
- Verify redirect destinations (login/signup/reset/settings) are correct/configurable; add constants if needed.
- Update prompt archive/docs if adjustments are made.

## Outcomes
- Auth forms have consistent loading/disabled states, non-revealing errors, and aligned redirects; any new strings are externalised.***
