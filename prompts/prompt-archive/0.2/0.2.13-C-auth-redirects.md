# Prompt Archive: 0.2.13-C — Auth Redirects & Flow Mapping

## Original Task
Ensure redirect targets are correct and configurable.

## Restatement
Define and verify redirect destinations for auth flows (login/signup/reset/settings/logout), make them configurable (constants), and ensure UX/messages match the flows.

## Phase/Sub-phase
0.2.13-C

## Clarifications
None yet.

## Plan
- Map redirect targets: login/signup → dashboard/home; reset → login; logout → login; settings save → settings.
- Add/config constants for these targets; use them in client redirects.
- Verify banners/messages reflect the redirect flow; update i18n if needed.

## Outcomes
- Redirect map defined and configurable; auth flows use consistent targets; prompt archived.***
