# Prompt Archive: 0.2.12-C — Frontend Wiring to Real Auth Endpoints

## Original Task
Wire frontend client to real endpoints; keep stub fallback for CI; update error handling and redirects.

## Restatement
Point the auth UI client (login/signup/reset/settings/logout) at the real Next.js auth routes, keep a lightweight stub fallback for CI/local if needed, and ensure error handling and redirects match the API responses.

## Phase/Sub-phase
0.2.12-C

## Clarifications
None yet.

## Plan
- Ensure auth client calls `/api/auth/*` routes for login/signup/reset/settings/logout with structured error handling.
- Keep a minimal stub/fallback path if the backend is unavailable in CI/local.
- Align redirects/messages with API outcomes; keep non-revealing auth errors.
- Update docs/changelog/prompt archive to note the wiring.

## Outcomes
- Frontend auth client wired to real `/api/auth/*` endpoints with structured error handling, CSRF headers, and existing redirects/messages; no localStorage tokens.  
- Stub fallback not required (using real routes); prompt archive updated.***
