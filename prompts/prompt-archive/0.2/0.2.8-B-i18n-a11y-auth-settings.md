# Prompt Archive: 0.2.8-B — i18n/A11y/Theming Check for Auth & Settings UI

## Original Task
Verify all auth/settings UI strings are externalised; apply a11y patterns; ensure consistent theming/layout.

## Restatement
Audit the auth/login/signup/reset/settings screens to ensure every user-facing string is pulled from shared i18n, a11y patterns are applied (labels, aria, focus/keyboard), and styling uses shared layout/token patterns instead of ad-hoc CSS. Scope includes documenting gaps and wiring fixes as needed.

## Phase/Sub-phase
0.2.8-B

## Clarifications
None yet.

## Plan
- Inventory strings across auth/settings UI; mark anything hardcoded for externalisation in shared i18n bundles.
- Check a11y: labels/aria, focus order, keyboard nav, error/success messaging patterns; add missing attributes or notes.
- Review theming/layout: ensure use of shared tokens/layout classes; remove ad-hoc colors/spacing if present.

## Outcomes
- Documented i18n/a11y/theming audit for auth/settings UI with identified fixes and wiring steps; ready to apply in code as needed.***
