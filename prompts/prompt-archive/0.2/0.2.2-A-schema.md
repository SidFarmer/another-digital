# Prompt Archive: 0.2.2-A — Auth Schema/Data Layer

## Original Task
Proceed with 0.2.2-A: Specify schemas for users, tenants (single-tenant placeholder), sessions/tokens, locale preference fields, consent timestamps.

## Restatement
Document the schema details for auth-related entities (users, tenants, sessions/tokens) including locale preferences and consent timestamps. Update data model docs (schemas/tables/ERD/migrations) as needed. No code changes.

## Phase/Sub-phase
0.2.2-A

## Clarifications
None yet.

## Plan
- Review current data model docs (schemas/tables/erd/migrations) for auth entities.
- Add/confirm fields for locale, consent, session/token metadata; clarify single-tenant placeholders.
- Keep scope to documentation; align with auth API/session lifecycle docs.

## Outcomes
- Reviewed data model docs: users include locale and consent fields; sessions include expiry/lastAccessed and session token ref; tenants noted as single-tenant placeholder; migrations list these fields.
- Existing docs (erd/schemas/tables/migrations) already cover the required auth fields; no additional updates needed.
- No code changes; tests N/A (docs review only).***
