# Prompt Archive: 0.2.10-A — Auth UI ↔ API Integration Plan

## Original Task
Plan wiring of auth UI to auth API, token storage, error handling, and redirect flows.

## Expanded Scope (Implementation Required)
Implement the auth UI → API wiring now (login/signup/reset/settings) using fetch calls, stubbed endpoints, and safe token/session handling. Add success/error messaging (aria-live, externalised strings), redirects, and stub consent/analytics flags. Plan and wire in lightweight telemetry hooks for auth events to be refined in 0.2.10-B.

## Restatement
Implement and document how the auth frontend (login/signup/reset/settings) connects to the auth API: request/response handling, secure token/session handling (prefer HTTP-only cookies; fallback header stub for now), error/success messaging, and redirects after auth actions. Include consent/analytics flags and placeholder telemetry emitters.

## Phase/Sub-phase
0.2.10-A

## Clarifications
None yet.

## Plan
- Define and implement client-side calls for login/signup/reset/settings: endpoints, payload shapes, expected responses/errors (aligned with `docs/api/auth-api.md`); stub backend with fetch mocks if needed.
- Handle tokens/sessions safely: prefer HTTP-only cookies; if using header stub, avoid localStorage; note CSRF considerations; pass locale/consent flags.
- Map and implement redirects: login/signup → dashboard; reset confirm → login; logout → login; settings save → settings with success banner.
- Implement messaging: non-revealing auth errors; aria-live banners/toasts; externalised strings.
- Add placeholder telemetry emitters for auth events (login/signup/reset/settings save) to be extended in 0.2.10-B.

## Outcomes
- Auth UI wired to auth API stubs with secure token handling approach, redirects, consent/analytics flags, messaging, and placeholder telemetry emitters; docs updated accordingly.***
