# Prompt Archive: 0.2.14-A — Locale Persistence & API/Telemetry Propagation

## Original Task
Persist locale (cookie/context) and pass locale in API requests/telemetry; ensure switcher updates UI.

## Restatement
Make the user’s selected locale stick (cookie + client context), send it along with auth/API requests and telemetry events, and ensure the locale switcher updates the UI consistently.

## Phase/Sub-phase
0.2.14-A

## Clarifications
None yet.

## Plan
- Store selected locale in a cookie and hydrate client context from it; default to en-US if missing.
- Include locale in auth/API requests and telemetry payloads.
- Ensure the locale switcher updates context, cookie, and triggers UI refresh; keep strings externalised.

## Outcomes
- Locale persists via cookie/context, propagates to API/telemetry, and the switcher updates UI consistently.***
