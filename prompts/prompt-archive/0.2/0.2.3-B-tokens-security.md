# Prompt Archive: 0.2.3-B — Token/Session Handling & Security

## Original Task
Proceed with 0.2.3-B: Define token/session handling approach (e.g., HTTP-only cookies/JWT) and security considerations.

## Restatement
Document the chosen token/session handling for 0.2 (HTTP-only cookies preferred for browser clients; token header support if needed), including expiry/rotation, CSRF protection, storage rules, and redaction. Scope is documentation only; no code changes.

## Phase/Sub-phase
0.2.3-B

## Clarifications
None yet.

## Plan
- Update auth docs to state the token/session approach and security considerations (cookies vs headers, CSRF, rotation, hashing session tokens, no localStorage).
- Keep changes to documentation; ensure alignment with standards (security, compliance, environment).

## Outcomes
- Updated `docs/api/auth-api.md` token/session handling: prefer HTTP-only secure cookies for browsers with CSRF protections; allow Authorization header if needed; forbid localStorage/sessionStorage for tokens; hash session_token if persisted; include locale and tenant context later; expiry/rotation noted.
- No code changes; tests N/A (docs update).***
