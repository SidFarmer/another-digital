# Prompt Archive: 0.2.1-B — Data Model Updates (Auth)

## Original Task
Proceed with 0.2.1-B: Update data model docs/ERD for users, tenants, sessions, locale preferences, consent flags.

## Restatement
Document the data model changes for auth foundations: users, tenants (single-tenant placeholder), sessions/tokens, locale preferences, and consent flags. Update data model docs/ERD/tables/migrations notes as needed. No code changes.

## Phase/Sub-phase
0.2.1-B

## Clarifications
None yet.

## Plan
- Review `docs/data-model/erd.md`, `docs/data-model/schemas.md`, `docs/data-model/tables.md`, and `docs/data-model/migrations.md` for current coverage.
- Add/update entries for users, tenants (single-tenant), sessions/tokens, locale preference fields, and consent flags.
- Keep scope to documentation; align with auth API/session lifecycle notes.

## Outcomes
- Updated data model docs for auth:
  - `docs/data-model/erd.md`: added consent flags to Users summary and session lifecycle notes (token ref, lastAccessed).
  - `docs/data-model/schemas.md`: added consent fields (consentGivenAt, consentVersion, analyticsOptIn) to Users; added session fields (lastAccessedAt, sessionToken).
  - `docs/data-model/tables.md`: reflected consent fields on users; added session last_accessed_at and session_token.
  - `docs/data-model/migrations.md`: noted initial tables should include locale/consent on users and session metadata.
- No code changes; tests N/A (docs update).***
