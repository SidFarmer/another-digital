# Prompt Archive: 0.2.5-A — I18n Infrastructure Plan

## Original Task
Proceed with 0.2.5-A: Define shared i18n package structure, per-app bundles, locale detection, and fallback logic.

## Restatement
Document the planned i18n infrastructure: shared i18n package location, per-app message bundles, locale detection strategy, fallback rules, and how apps (auth/settings and beyond) consume it. Scope is documentation/plan only; no code changes yet.

## Phase/Sub-phase
0.2.5-A

## Clarifications
None yet.

## Plan
- Outline shared i18n package structure and namespaces; note per-app bundles and how they load.
- Document locale detection/fallback behavior and default locale.
- Keep changes to documentation; align with existing i18n notes for auth/settings.

## Outcomes
- Added i18n infrastructure notes to `docs/frontend/README.md`: shared `packages/i18n` planned for provider/hooks and per-app bundles, detection order (user setting → persisted preference → Accept-Language → default `en-US`), fallback to default on missing keys, and bundle splitting by route/surface; auth/settings namespaces already defined.
- No code changes; tests N/A (docs update).***
