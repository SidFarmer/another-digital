# Prompt Archive: 0.2.10-B — Auth Telemetry Events

## Original Task
Define telemetry events for auth (login/signup/reset/settings) with locale/tenant metadata.

## Expanded Scope (Implementation Required)
Implement lightweight telemetry emitters for auth events (login, signup, reset request, settings save) carrying locale/tenant metadata and flags (consent/analytics). Keep backend ingestion stubbed/minimal to avoid breaking CI.

## Phase/Sub-phase
0.2.10-B

## Clarifications
None yet.

## Plan
- Define event names and payload shapes (locale, tenant?, consent/analytics flags where relevant).
- Implement emitter hook/module; call from auth flows (login/signup/reset/settings) using shared i18n locale.
- Keep ingestion stubbed/no-op for now; ensure no PII in payloads.

## Outcomes
- Auth telemetry emitters implemented and called from auth flows with locale/tenant metadata and consent/analytics flags; backend ingestion remains stubbed.***
