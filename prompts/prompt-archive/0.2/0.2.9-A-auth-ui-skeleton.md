# Prompt Archive: 0.2.9-A — Auth UI Skeleton Implementation (Login/Signup/Reset/Settings)

## Original Task
Deliver auth frontend skeleton (login/signup/reset/settings) using shared layout/ui/i18n with locale switcher and consent/analytics toggle (stubs acceptable).

## Restatement
Implement the auth screens (login, signup, reset, settings) as runnable skeletons using the shared layout, i18n helpers, and styling tokens. Include locale switcher placement and a consent/analytics toggle (stubbed). Ensure a11y patterns (labels, aria-live for messages, focus order) and externalised strings.

## Phase/Sub-phase
0.2.9-A

## Clarifications
None yet.

## Plan
- Wire login/signup/reset/settings pages to shared layout and i18n catalog; ensure locale switcher is present.
- Include consent/analytics toggle in signup/settings; keep stub handlers.
- Apply a11y patterns: labels, aria-describedby, non-revealing errors, focus order, aria-live placeholders.

## Outcomes
- Auth screens implemented as skeletons with shared layout/i18n/styling, locale switcher, and consent/analytics toggle; ready for future API wiring.
- Added aria-live regions on auth/settings forms for future success/error messaging; ensured buttons reference message regions for accessibility.***
