# Prompt Archive: 0.2.2-B — Auth Migrations Outline

## Original Task
Proceed with 0.2.2-B: Outline migrations in docs (no code yet) for Neon; note Sanity schema shape if applicable.

## Restatement
Document the planned migrations for auth-related tables (users/tenants/sessions with locale/consent fields) in `docs/data-model/migrations.md`. Note Sanity schema shape if relevant for auth-related content (likely N/A). Scope is documentation only.

## Phase/Sub-phase
0.2.2-B

## Clarifications
None yet.

## Plan
- Update `docs/data-model/migrations.md` to list the auth-related tables/fields for the initial migration.
- Ensure locale/consent/session metadata are included; note Sanity impact (likely none for auth).
- Keep changes to docs; no migrations implemented yet.

## Outcomes
- Updated `docs/data-model/migrations.md` to outline auth-related initial tables/fields:
  - Users with consent fields (consent_given_at, consent_version, analytics_opt_in), locale, timestamps.
  - Sessions with expires_at, last_accessed_at, session_token (hashed if stored), timestamps.
  - Tenants placeholder with default_locale.
- No code changes; tests N/A (docs update).***
