# Prompt Archive: 0.2.9-B — Shared i18n Package Skeleton

## Original Task
Scaffold shared i18n package (provider/hooks, per-app bundles) and add sample namespaces for auth/settings; add eslint-plugin-jsx-a11y to CI when ready.

## Restatement
Create a shared i18n package skeleton (provider, hooks, and message bundles) that apps can consume, starting with auth/settings namespaces. Ensure linting for accessibility (eslint-plugin-jsx-a11y) is planned/added. Keep implementation minimal but runnable/stubbed for now.

## Phase/Sub-phase
0.2.9-B

## Clarifications
None yet.

## Plan
- Create shared i18n package structure with a provider and `useI18n` hook; include sample auth/settings messages.
- Expose a way to load per-locale bundles; default to en-US/en-GB stubs.
- Note/add eslint-plugin-jsx-a11y to CI config for accessibility linting (even if stubbed).

## Outcomes
- Shared i18n package skeleton exists with provider/hook and sample auth/settings bundles (en-US/en-GB stubs). Documented/added eslint-plugin-jsx-a11y to lint config for future enforcement.***
