# Prompt Archive: 0.2.5-C — i18n Guidelines for New Product Areas

## Original Task
Proceed with 0.2.5-C: Establish guidelines for adding new product areas so all user-facing strings are externalised by default and reuse shared i18n patterns.

## Restatement
Document clear guidelines for teams adding new surfaces/apps: all user-facing strings must be externalised into shared i18n namespaces, follow naming conventions, and load via the shared i18n provider/hooks. Scope is documentation only.

## Phase/Sub-phase
0.2.5-C

## Clarifications
None yet.

## Plan
- Add a guideline note to frontend/i18n docs on externalising strings for new product areas using shared i18n patterns/namespaces.
- Keep scope to documentation; no code changes.

## Outcomes
- Added guideline to `docs/frontend/README.md`: new product areas must externalise all user-facing strings into shared i18n namespaces, add per-area bundles under `packages/i18n`, avoid inline strings, and reuse patterns used for auth/settings (labels, errors, success, hints).
- No code changes; tests N/A (docs update).***
