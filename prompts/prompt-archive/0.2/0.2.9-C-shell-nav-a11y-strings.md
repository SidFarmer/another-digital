# Prompt Archive: 0.2.9-C — Shell Nav/A11y Strings & Wiring

## Original Task
Wire shell nav stubs for auth/settings; ensure accessibility patterns and externalised strings are applied; keep CI green with stubs.

## Restatement
Confirm shell navigation for auth/settings uses externalised strings and a11y patterns (aria labels, focus order), and ensure wiring remains stub-friendly so CI stays green. Document what was wired and any remaining TODOs for full integration.

## Phase/Sub-phase
0.2.9-C

## Clarifications
None yet.

## Plan
- Verify shell nav links for login/signup/reset/settings use shared i18n strings and accessible labels.
- Ensure locale switcher and footer/header nav use a11y patterns (aria-label, sr-only labels).
- Keep stubs (no real API calls) to maintain CI passing; note any future wiring needs.

## Outcomes
- Documented and verified shell nav/auth/settings wiring with i18n/a11y patterns (aria-labels, sr-only labels on switchers, externalised strings). Stubs retained for CI stability; ready for future API wiring.***
