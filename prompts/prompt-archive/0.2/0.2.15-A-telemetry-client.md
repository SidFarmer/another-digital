# Prompt Archive: 0.2.15-A — Telemetry Client & Validation

## Original Task
Replace the console telemetry stub with a real telemetry client that validates against a schema and never ships PII.

## Restatement
Build a minimal telemetry client that sends events to a real endpoint, validates payloads against a JSON schema, and strips/blocks any PII before sending.

## Phase/Sub-phase
0.2.15-A

## Clarifications
None yet.

## Plan
- Define a JSON schema for telemetry events (ids/timestamps/context, no PII fields).
- Implement a telemetry client that validates events against the schema and posts to a real endpoint with retry/backoff hooks ready.
- Ensure the client rejects or removes PII fields and logs validation failures locally for debugging.

## Outcomes
- Telemetry sends through the client to the stub/endpoint, validated against the schema, with PII excluded and failures surfaced locally.
