# Prompt Archive: 0.1.3-B — Standard Commands (Lint/Test/Typecheck/Format)

## Original Task
Proceed with 0.1.3-B: document and align standard commands for lint, test, typecheck, and format (even if stubbed) per `docs/architecture/tooling.md` and the code-style/testing standards. No implementation code yet.

## Restatement
Ensure the workspace has a clear, documented set of commands for lint/test/typecheck/format/build. Update `docs/architecture/tooling.md` (or related docs) so these commands are explicit and match the root `package.json` stubs. Keep commands non-failing until tooling is wired; no app/package code changes.

## Phase/Sub-phase
0.1.3-B

## Clarifications
None.

## Plan
- Review current `docs/architecture/tooling.md` and root `package.json` stub scripts.
- Confirm command naming and intended tools per standards (code-style, testing).
- Update docs to explicitly list commands and note stub behavior; keep aligned with root scripts.

## Outcomes
- Updated `docs/architecture/tooling.md` to explicitly mark lint/test/typecheck/format/build as stub commands that echo placeholders until tooling is wired, aligned with root `package.json`.
- No code changes; tests N/A (docs-only).***
