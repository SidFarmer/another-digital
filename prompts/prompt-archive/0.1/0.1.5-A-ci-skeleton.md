# Prompt Archive: 0.1.5-A — CI Skeleton

## Original Task
Start 0.1.5-A: add minimal CI config to run lint/test/typecheck placeholders.

## Restatement
Ensure CI is configured to run the standard lint/test/typecheck commands (stubs are fine) on the appropriate branches/PRs. Align with existing tooling versions and workspace layout. No product code changes beyond CI config.

## Phase/Sub-phase
0.1.5-A

## Clarifications
None yet.

## Plan
- Review current `.github/workflows/ci.yml` and align triggers/commands with the standard scripts.
- Ensure pnpm/node setup matches pinned versions (Node 20.10.0, pnpm 10.19.0).
- Keep commands stub-friendly; avoid failures until real tooling is wired.
- Update archive with outcomes; scope limited to CI config/docs if needed.

## Outcomes
- Updated `.github/workflows/ci.yml` to run on push to `main`, `dev`, and `test` (plus PRs) and to execute the standard stub commands using Node 20.10.0 and pnpm 10.19.0.
- Workflow retains manifest detection to skip if no `package.json` (not expected now that root manifest exists).
- Commands remain stub-friendly; no product code changes. Tests: CI config only.***
