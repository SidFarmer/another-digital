# Prompt Archive: 0.1.5-C — CI Notes for Migrations/Versioning

## Original Task
Proceed with 0.1.5-C: note how migrations/versioning will be tracked in CI once introduced.

## Restatement
Add documentation to describe how database migrations/versioning would be integrated into CI when code lands (e.g., which commands to run, where to place migration checks). No migration tooling yet; capture the plan only.

## Phase/Sub-phase
0.1.5-C

## Clarifications
None yet.

## Plan
- Update `docs/workflows/ci-cd.md` with a brief note on future migration/versioning checks (when schemas exist).
- Keep scope to documentation only; no code changes or migration tools added now.

## Outcomes
- Added a note to `docs/workflows/ci-cd.md` for future migration/versioning checks: when schemas/migrations exist, CI should run migration validation (e.g., lint/validate, apply up/down in temp DB). No tooling added yet. Tests: N/A (docs update).***
