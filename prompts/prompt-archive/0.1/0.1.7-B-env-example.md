# Prompt Archive: 0.1.7-B — Sample `.env.example` Spec

## Original Task
Proceed with 0.1.7-B: Provide sample `.env.example` spec in docs (no secrets).

## Restatement
Review the current `.env.example` and update it (if needed) to cover required keys for this phase (auth/API stub, Neon, Sanity, frontend URL, analytics stub, payments placeholder) without secrets. Ensure it matches the environment standard and tooling docs.

## Phase/Sub-phase
0.1.7-B

## Clarifications
None yet.

## Plan
- Audit the existing `.env.example` against environment requirements.
- Add/remove keys to reflect current scope (no secrets).
- Keep scope to the sample env file; no code changes.

## Outcomes
- Updated `.env.example`:
  - Added `APP_PORT` for backend binding.
  - Added `PUBLIC_LOCALE_DEFAULT` for frontend defaults.
  - Retained existing placeholders for API, Neon, Sanity, analytics stub, and payments placeholder.
- No secrets added; scope limited to sample env. Tests: N/A.***
