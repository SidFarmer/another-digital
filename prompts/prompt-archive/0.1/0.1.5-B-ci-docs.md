# Prompt Archive: 0.1.5-B — Document CI Expectations

## Original Task
Proceed with 0.1.5-B: document CI expectations in `docs/workflows/ci-cd.md` (trigger, required checks).

## Restatement
Update `docs/workflows/ci-cd.md` to describe current CI triggers (main/dev/test pushes, PRs) and required checks (lint/test/typecheck stubs). Align with pinned tooling (Node 20.10.0, pnpm 10.19.0) and note that commands are stubbed until code exists. No product code changes.

## Phase/Sub-phase
0.1.5-B

## Clarifications
None yet.

## Plan
- Review `docs/workflows/ci-cd.md` and add CI trigger/check expectations reflecting the current workflow.
- Note stub commands and pinned tooling versions.
- Keep scope to documentation only.

## Outcomes
- Updated `docs/workflows/ci-cd.md` to reflect current CI expectations:
  - Triggers: PRs; pushes to `main`, `dev`, `test`.
  - Steps: pnpm install + stub lint/test/typecheck commands.
  - Tooling versions: Node 20.10.0, pnpm 10.19.0.
  - Gates noted for when real checks exist.
- No product code changes. Tests: N/A (docs update).***
