# Prompt Archive: 0.1.3-D — Stack Choices & Frontend/Backend Boundaries

## Original Task
Proceed with 0.1.3-D: document stack choices and separation (backend on Node/Express with Neon; content via Sanity; Turborepo/pnpm for workspace; Husky for hooks; frontend/backend deployed separately—e.g., Vercel for frontend, Render or equivalent for backend) and clarify boundaries between frontend/backend packages/apps.

## Restatement
Update the tooling/architecture docs to capture the stack decisions and boundaries: Node/Express backend with Neon, Sanity for content, pnpm + Turborepo for the workspace, Husky for hooks (planned), and separate deployment targets for frontend vs backend (e.g., Vercel frontend, Render or equivalent backend). Clarify how frontend apps vs backend services/packages are separated and how shared packages are consumed. No product code changes.

## Phase/Sub-phase
0.1.3-D

## Clarifications
None yet.

## Plan
- Review `docs/architecture/tooling.md` (and related docs) and add explicit stack choices (Node/Express, Neon, Sanity, pnpm/Turborepo, Husky).
- Document frontend/backend deployment separation (e.g., Vercel vs Render) and package/app boundaries.
- Keep scope to documentation updates only; no code changes.

## Outcomes
- Updated `docs/architecture/tooling.md` to document stack choices and boundaries:
  - Backend: Node/Express; Neon for structured data; Sanity for content.
  - Workspace: pnpm + Turborepo (install via `pnpm dlx turbo@1.12.3`); Husky planned for format/lint/typecheck once tooling exists.
  - Deployment targets: frontend apps (Next.js planned) to Vercel; backend services to Render or equivalent; keep backend code out of frontend apps.
  - Shared packages under `/packages/*` (types/utils/layout/ui/auth/permissions/entities/events/analytics/content/community/profile/workspace/plugins/api/config/theme/i18n); frontends consume UI/shared packages; backends consume non-UI/shared logic.
  - Storage split: Neon vs Sanity; adapters/clients belong in appropriate packages, not UI packages.
- No code changes; tests N/A (docs update).***
