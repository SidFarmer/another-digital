# Prompt Archive: 0.1.2-B — Baseline Workspace & Tooling Config

## Original Task
Proceed with 0.1.2-B: add baseline package/workspace/tooling config files per the documented stack (Node + pnpm + Turborepo, lint/test/typecheck stubs, env sample). No app/service code yet.

## Restatement
Prepare initial workspace/config scaffolding to match `docs/architecture/monorepo-structure.md` and `docs/architecture/tooling.md`: ensure `pnpm-workspace.yaml` aligns with the monorepo layout, add root `package.json` with scripts for lint/test/typecheck (stubs), and include baseline configs/placeholders if specified (e.g., `.nvmrc`, `.npmrc`, `.env.example`). Do not add implementation code—only configuration scaffolding.

## Phase/Sub-phase
0.1.2-B

## Clarifications
None yet.

## Plan
- Review `docs/architecture/tooling.md` and related standards to confirm required configs (pnpm/Turborepo, Node version, scripts).
- Create/update baseline workspace and root configs (e.g., `package.json` with stub scripts, align `pnpm-workspace.yaml` with current layout).
- Keep changes minimal (no app/package code); document any assumptions or gaps.

## Outcomes
- Added root `package.json` (private) with `packageManager: pnpm@10.19.0`, `engines.node >=20.10.0`, and stub scripts for lint/test/typecheck/format/build (echo placeholders to avoid failing CI until tooling is wired).
- Verified existing configs: `pnpm-workspace.yaml` already targets `apps/*` and `packages/*` (now present); `.nvmrc` pins Node 20.10.0; `.npmrc` pins pnpm 10.19.0 and peer settings; `.env.example` present.
- No app/package code added; no doc changes needed. Tests: N/A (config scaffolding).***
